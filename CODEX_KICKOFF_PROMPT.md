# Codex Kickoff Prompt — Skill Gomoku

请在当前 GitHub 仓库中开始开发原生 iPhone / iPad 游戏 **《技能五子棋》**。

## 先阅读

在修改代码前，完整阅读：

- `docs/PRD.md`
- `docs/reference/skill-cards-a.png`
- `docs/reference/skill-cards-b.png`
- `docs/reference/web-layout-reference.png`
- `GITHUB_SETUP.md`
- `README.md`

参考图片只用于理解以下布局思想：

- iPad 左右各显示一位玩家
- 当前玩家高亮
- 非当前玩家灰化
- 技能卡显示名称、作用、冷却和可用状态

不要复制参考网页的视觉资产、图标、配色或组件。最终设计需要原创、现代、克制，不使用传统木纹、水墨或仿古围棋风格。

---

## 产品与技术约束

- 建立原生 Universal iOS / iPadOS App。
- 使用 SwiftUI。
- 最低目标建议为 iOS / iPadOS 17.0。
- 使用 SwiftData 保存玩家、对战记录和未完成对局。
- 使用 PhotosPicker 选择头像。
- 支持通过设备相机拍摄头像。
- 不使用 Capacitor、React Native、Flutter 或 WebView。
- 不实现 Bot、AI 对手、在线联机、账号、广告、内购或后端。
- 不引入第三方运行时依赖，除非确有必要，并在 PR 中解释。
- 游戏引擎必须与 SwiftUI 解耦。
- 随机行为必须支持固定 seed，以便测试和恢复。
- 所有用户数据只保存在本地。
- UI 字符串以中文为首要语言，但使用可本地化结构。

---

## 本次任务范围

本次只完成 **Phase 0 + Phase 1 的可运行基础版本**。不要一次性实现全部九项技能。

### 必须完成

1. 建立清晰的原生 Xcode / SwiftUI 工程。
2. 建立 PRD 建议的目录和模块边界，可根据工程实践小幅优化。
3. 实现纯 Swift 游戏引擎：
   - 15 × 15 棋盘
   - 两位玩家轮流落子
   - 合法落子验证
   - 横、竖、两种斜线的五连检测
   - 六连及以上按 V1 规则获胜
   - 平局
   - 对局结束后拒绝继续行动
4. 实现玩家档案：
   - 姓名
   - 默认头像
   - PhotosPicker
   - 相机入口
   - 本地持久化
5. 实现页面：
   - 首页
   - 模式选择
   - 玩家选择
   - 对局设置
   - 经典五子棋游戏页
   - 对局结算
6. 实现 iPad 和 iPhone 的响应式布局：
   - iPad 横屏：玩家一在左、棋盘居中、玩家二在右
   - 当前玩家高亮
   - 非当前玩家灰化
   - iPhone 竖屏：双方摘要卡固定在棋盘上下
7. 实现未完成经典对局的自动保存和恢复。
8. 为后续技能系统预留接口和 UI 区域，但本次不实现技能效果。
9. 建立设计系统：
   - 颜色 token
   - 字体层级
   - 间距
   - 卡片
   - 玩家状态
   - 棋子和棋盘样式
10. 编写核心 XCTest。

---

## 设计方向

关键词：

- 现代数字桌游
- 专业
- 大气
- 家庭友好
- 精致但不浮夸

避免：

- 木纹
- 水墨
- 竹子
- 传统围棋黑白棋子作为唯一视觉
- 棋牌室风格
- 大量金色
- 过度游戏化奖励 UI

建议使用深色背景、浅石灰棋盘、磨砂卡片、玩家一蓝色、玩家二珊瑚橙，并保证颜色之外还有形状或纹理区分。

---

## 代码质量要求

- 使用小而明确的类型。
- 避免把游戏规则写进 View。
- 使用 `GameAction`、`GameState`、`RuleEngine` 或等价结构。
- Win detection 必须可独立单元测试。
- 随机数接口先定义，即使 Phase 1 尚未使用技能。
- 对持久化和相机使用协议或服务层，方便替换和测试。
- 不要使用大而全的单一 ViewModel。
- 对关键设计决策添加简短注释，不写冗长解释性注释。
- 保持编译警告为零。
- 不提交用户个人 Xcode 数据、DerivedData、签名证书或 secrets。

---

## 测试最低要求

至少添加以下单元测试：

- 第一手落子合法
- 重复位置落子非法
- 双方正确交替
- 横向五连
- 纵向五连
- 正斜线五连
- 反斜线五连
- 六连获胜
- 游戏结束后不能继续落子
- 棋盘填满时平局
- GameState 可编码和恢复
- 固定玩家资料可持久化

如相机在当前执行环境无法自动测试，使用协议抽象并提供模拟实现。

---

## 工作方式

1. 先检查仓库当前状态。
2. 写一个简短实施计划。
3. 逐步创建工程和代码。
4. 经常运行可用的测试和静态检查。
5. 不因当前环境没有 Xcode 而伪造“构建成功”。
6. 如果环境无法运行 Xcode：
   - 仍应创建完整、合理的源代码和测试结构；
   - 明确列出未能执行的验证；
   - 提供在本地 Xcode 中运行的具体命令或步骤。
7. 对 PRD 中未锁死的细节做合理决定，并记录在 `docs/IMPLEMENTATION_DECISIONS.md`。
8. 不要在未说明的情况下扩大范围。

---

## 本次交付格式

完成后提供：

- 修改文件摘要
- 架构说明
- 已实现页面
- 已实现规则
- 测试结果
- 未能在当前环境验证的项目
- 下一阶段建议
- 已知问题

请以一个可审查的 feature branch 完成本次工作，建议分支名：

`codex/phase-0-1-foundation`

建议提交信息：

`feat: build native app foundation and classic gomoku mode`
